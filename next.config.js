/** @type {import('next').NextConfig} */
const nextConfig = {
    trailingSlash: true,
    images: {
        unoptimized: true,
    },
}

module.exports = nextConfig

// trigger reload

// triggered reload 2
